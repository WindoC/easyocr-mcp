[project]
name = "easyocr-mcp"
version = "0.1.0"
description = "MCP server that provides OCR tools using EasyOCR"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "easyocr>=1.7.2",
    "pillow>=8.0.0",
    "requests>=2.25.0",
    "numpy>=1.21.0",
    "mcp"
]
